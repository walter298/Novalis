The package boost-redis is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS redis)
    target_link_libraries(main PRIVATE Boost::redis)

or the generated cmake configs via:

    find_package(boost_redis REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::redis)
