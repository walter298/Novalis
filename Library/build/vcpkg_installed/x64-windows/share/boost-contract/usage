The package boost-contract is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS contract)
    target_link_libraries(main PRIVATE Boost::contract)

or the generated cmake configs via:

    find_package(boost_contract REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::contract)
