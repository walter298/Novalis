The package boost-metaparse is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS metaparse)
    target_link_libraries(main PRIVATE Boost::metaparse)

or the generated cmake configs via:

    find_package(boost_metaparse REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::metaparse)
