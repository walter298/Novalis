The package boost-msm is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS msm)
    target_link_libraries(main PRIVATE Boost::msm)

or the generated cmake configs via:

    find_package(boost_msm REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::msm)
