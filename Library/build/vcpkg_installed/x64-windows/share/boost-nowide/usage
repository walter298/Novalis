The package boost-nowide is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS nowide)
    target_link_libraries(main PRIVATE Boost::nowide)

or the generated cmake configs via:

    find_package(boost_nowide REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::nowide)
