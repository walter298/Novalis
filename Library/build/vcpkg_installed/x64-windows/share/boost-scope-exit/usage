The package boost-scope-exit is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS scope_exit)
    target_link_libraries(main PRIVATE Boost::scope_exit)

or the generated cmake configs via:

    find_package(boost_scope_exit REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::scope_exit)
