The package boost-fiber is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS fiber)
    target_link_libraries(main PRIVATE Boost::fiber)

or the generated cmake configs via:

    find_package(boost_fiber REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::fiber)
