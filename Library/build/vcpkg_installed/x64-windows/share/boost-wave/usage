The package boost-wave is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS wave)
    target_link_libraries(main PRIVATE Boost::wave)

or the generated cmake configs via:

    find_package(boost_wave REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::wave)
