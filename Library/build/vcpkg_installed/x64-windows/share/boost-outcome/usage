The package boost-outcome is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS outcome)
    target_link_libraries(main PRIVATE Boost::outcome)

or the generated cmake configs via:

    find_package(boost_outcome REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::outcome)
