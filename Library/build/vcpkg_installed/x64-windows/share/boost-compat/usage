The package boost-compat is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS compat)
    target_link_libraries(main PRIVATE Boost::compat)

or the generated cmake configs via:

    find_package(boost_compat REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::compat)
