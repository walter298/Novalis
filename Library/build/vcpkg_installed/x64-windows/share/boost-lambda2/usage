The package boost-lambda2 is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS lambda2)
    target_link_libraries(main PRIVATE Boost::lambda2)

or the generated cmake configs via:

    find_package(boost_lambda2 REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::lambda2)
