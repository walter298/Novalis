The package boost-rational is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS rational)
    target_link_libraries(main PRIVATE Boost::rational)

or the generated cmake configs via:

    find_package(boost_rational REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::rational)
