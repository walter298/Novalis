The package boost-histogram is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS histogram)
    target_link_libraries(main PRIVATE Boost::histogram)

or the generated cmake configs via:

    find_package(boost_histogram REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::histogram)
