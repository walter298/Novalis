The package boost-sort is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS sort)
    target_link_libraries(main PRIVATE Boost::sort)

or the generated cmake configs via:

    find_package(boost_sort REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::sort)
