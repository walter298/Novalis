The package boost-convert is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS convert)
    target_link_libraries(main PRIVATE Boost::convert)

or the generated cmake configs via:

    find_package(boost_convert REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::convert)
