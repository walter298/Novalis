The package boost-log is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS log)
    target_link_libraries(main PRIVATE Boost::log)

or the generated cmake configs via:

    find_package(boost_log REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::log)
