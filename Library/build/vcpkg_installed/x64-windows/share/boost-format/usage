The package boost-format is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS format)
    target_link_libraries(main PRIVATE Boost::format)

or the generated cmake configs via:

    find_package(boost_format REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::format)
