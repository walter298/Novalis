The package boost-units is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS units)
    target_link_libraries(main PRIVATE Boost::units)

or the generated cmake configs via:

    find_package(boost_units REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::units)
