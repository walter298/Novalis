The package boost-vmd is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS vmd)
    target_link_libraries(main PRIVATE Boost::vmd)

or the generated cmake configs via:

    find_package(boost_vmd REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::vmd)
