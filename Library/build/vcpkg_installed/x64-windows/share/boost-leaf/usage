The package boost-leaf is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS leaf)
    target_link_libraries(main PRIVATE Boost::leaf)

or the generated cmake configs via:

    find_package(boost_leaf REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::leaf)
