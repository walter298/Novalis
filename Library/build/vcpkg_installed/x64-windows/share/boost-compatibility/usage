The package boost-compatibility is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS compatibility)
    target_link_libraries(main PRIVATE Boost::compatibility)

or the generated cmake configs via:

    find_package(boost_compatibility REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::compatibility)
