#pragma once

#include "SharedBuffer.h"

namespace nv {
	/*class DynamicSharedBuffer {
	private:
		struct Bucket {
			size_t size = 0;
			SharedBuffer buff;
		};
		std::vector<Bucket> m_buckets;
		size_t 
	public:
		DynamicSharedBuffer() {

		}
	};*/
}